# Instala espeak-ng (motor de voz offline) además del setup de Node.
# Se usa para generar el audio "Ticket N" de la pantalla pública, porque el
# navegador Silk del Fire TV no trae voz propia (speechSynthesis vacío).
[phases.setup]
aptPkgs = ["espeak-ng"]
